template(name="Person")
  .person
    if photo
      div
        img.responsive-img.player-photo(src=photo)
    else
      div
        img.responsive-img(src="/img/people/default.jpg")
    p {{profile.name}}
    p {{profile.firstName}}
    if hasRole "admin"
      unless isImpersonating
        button.btn-flat(data-impersonate="{{_id}}") Impersonate
